development:
    driver: postgres
    open: user=gvs password=gvs dbname=gvs sslmode=disable

test:
    driver: postgres
    open: user=gvs password=gvs dbname=gvs_test sslmode=disable

production:
    driver: postgres
    open: $DATABASE_URL